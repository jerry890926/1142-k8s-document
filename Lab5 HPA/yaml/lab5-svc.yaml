apiVersion: v1
kind: Service
metadata:
  name: lab5-svc
  namespace: lab5
spec:
  type: NodePort
  selector:
    app: lab5-pod
  ports:
  - name: http
    port: 80
    targetPort: 80
    nodePort: 30085

